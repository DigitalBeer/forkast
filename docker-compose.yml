# docker-compose.yml
#
# Primary use: local testing of the Docker image before pushing to Unraid.
# Also serves as documentation for every environment variable the container needs.
#
# Usage:
#   1. Copy .env.docker.example to .env.docker and fill in your values.
#   2. docker compose --env-file .env.docker up --build
#
# Local testing with auth:
#   Create .env.docker.local (see .env.docker.local.example) to override
#   runtime env vars. Docker Compose picks this file up automatically if present.

services:
  forkast:
    image: ${DOCKER_IMAGE_NAME:-forkast}:${DOCKER_IMAGE_TAG:-latest}
    build:
      context: .
      # NEXT_PUBLIC_* vars must be passed at BUILD time — they are inlined
      # into the JavaScript bundle by the Next.js compiler.
      # For local auth testing, use scripts/build.ps1 -Local or create
      # .env.docker.local with NEXT_PUBLIC_APP_URL=http://localhost:3000.
      args:
        NEXT_PUBLIC_SUPABASE_URL: ${NEXT_PUBLIC_SUPABASE_URL}
        NEXT_PUBLIC_SUPABASE_ANON_KEY: ${NEXT_PUBLIC_SUPABASE_ANON_KEY}
        NEXT_PUBLIC_SENTRY_DSN: ${NEXT_PUBLIC_SENTRY_DSN}
        NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
        NEXT_PUBLIC_APP_VERSION: ${NEXT_PUBLIC_APP_VERSION:-0.1.0}
    ports:
      - '${HOST_PORT:-3000}:3000'
    # Runtime-only secrets — these are never baked into the image.
    environment:
      NODE_ENV: production
      # Supabase (server-side admin access)
      SUPABASE_SERVICE_ROLE_KEY: ${SUPABASE_SERVICE_ROLE_KEY}
      EDGE_SERVICE_ROLE_KEY: ${EDGE_SERVICE_ROLE_KEY}
      # Stripe
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY}
      STRIPE_PRICE_ID: ${STRIPE_PRICE_ID}
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET}
    # Optional local overrides for runtime env vars (Docker Compose v2.24+)
    env_file:
      - path: .env.docker.local
        required: false
    restart: unless-stopped
    healthcheck:
      test:
        ['CMD-SHELL', 'wget -qO- http://localhost:3000/api/health || exit 1']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
